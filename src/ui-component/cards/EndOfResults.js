import { Typography } from '@mui/material';

const EndOfResults = () => {
    return (
        <Typography sx={{ pt: 2 }} component="h4" variant="h4" align="center" color="text.primary" gutterBottom>
            End of Results
        </Typography>
    );
};

export default EndOfResults;
